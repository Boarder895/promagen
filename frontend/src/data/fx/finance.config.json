{
  "$schema": "https://promagen/schemas/finance.config.schema.json",
  "version": 1,
  "provider": {
    "name": "fx-live",
    "base": "${FX_API_BASE}",
    "key": "${FX_API_KEY}",
    "timeoutMs": 800,
    "schedule": "hourly-staggered"
  },
  "pairs": "*",
  "ribbon": {
    "defaultFreePairCount": "auto"
  },
  "notes": "pairs='*' means: allow all pair ids from src/data/fx/pairs.json. UI never hard-codes. ribbon.defaultFreePairCount: 'auto' uses all isDefaultFree pairs from fx.pairs.json; set a number to force a fixed count (e.g. 8, 10)."
}
