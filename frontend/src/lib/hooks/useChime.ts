// @/lib/hooks/useChime.ts
// Legacy bridge — re-export the canonical hook. Do not add config here.
export { useChime } from '../../hooks/useChime';
export type { UseChimeOptions, UseChimeReturn } from '../../hooks/useChime';

