# Promagen

Monorepo: `frontend/` (Next.js 14, React 18, TypeScript) and future workspaces.

## Requirements
- Node **22.x**
- pnpm **10.12.0** (workspace-aware)
- Git

## First-time setup
```powershell
# from REPO ROOT
pnpm -w install
