## Summary

<!-- What does this PR do, in 1–3 sentences? -->

## Change List (ADD / REPLACE / REMOVE)

- ADD:
- REPLACE:
- REMOVE:

**Existing features preserved:** Yes/No (Yes is the default expectation)

## Docs Gate (required)

- [ ] Docs Gate: Yes/No + Target doc + insertion point

**Authority:** `C:\Users\Proma\Desktop\Promagen Files\Best_Working_Practice.md` → “Docs-first gate (no code until docs are read + doc-delta captured)”

### Doc Delta (paste-ready)

- **Docs read:** Yes/No (list which of the authority docs were read)
- **Doc updates required:** Yes/No

If **Yes**:

- **Target doc (full path):**
- **Reason (what drift/contract changed):**
- **Exact insertion point (heading or line range):**
- **Paste-ready text to insert (full block):**

## Implementation Notes

<!-- Optional: key design decisions, trade-offs, and anything that might surprise reviewers -->

## Tests / Verification

Tick what you actually ran.

- [ ] `pnpm run lint -- --cache` (run from `frontend/`)
- [ ] `pnpm run typecheck` (run from `frontend/`)
- [ ] `pnpm run test:ci` (run from `frontend/`)
- [ ] `pnpm build` (run from `frontend/`)

## Screenshots / Evidence

<!-- UI screenshots, logs, trace snapshots, etc. -->

## Risk & Rollback

- **Risk level:** Low / Medium / High
- **Rollback plan:**

## Follow-ups

<!-- Anything intentionally out of scope for this PR -->
