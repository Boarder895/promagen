// BACKEND · Prisma schema
// File: C:\Users\Martin Yarnold\Projects\promagen\api\prisma\schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model ProviderSnapshotBatch {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  snapshots ProviderSnapshot[]
}

model Provider {
  id         String             @id @db.VarChar(64) // e.g. "openai", "i23rf"
  name       String
  createdAt  DateTime           @default(now())
  updatedAt  DateTime           @updatedAt
  overrides  ProviderOverride[] // relation back to overrides
}

model ProviderSnapshot {
  id         String                 @id @default(cuid())
  batchId    String
  providerId String                 // matches our registry id (e.g. "openai")
  status     String                 // "ok" | "degraded" | "unavailable"
  latencyMs  Int?
  score      Int                    // 0..100 auto (no manual adjustment)
  note       String?
  createdAt  DateTime               @default(now())

  batch ProviderSnapshotBatch @relation(fields: [batchId], references: [id], onDelete: Cascade)

  @@index([providerId])
  @@index([batchId])
}

model ProviderOverride {
  id              Int       @id @default(autoincrement())

  providerId      String
  provider        Provider  @relation(fields: [providerId], references: [id], onDelete: Cascade)

  // your fields (keep them)
  scoreAdjustment Int       @default(0)
  isHardOverride  Boolean   @default(false)
  finalScore      Int?
  notes           String?
  updatedBy       String?
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt

  @@index([providerId])
}




